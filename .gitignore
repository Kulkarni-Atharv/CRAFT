# ── Python ────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# ── Virtual environments ───────────────────────
venv/
.venv/
env/
craft_venv/

# ── Model weights ─────────────────────────────
# .pth files are large — keep off git
craft_ocr_pipeline/models/*.pth
craft_ocr_pipeline/models/*.pt
# .onnx files are tracked so CM5 can git pull them directly

# ── Pipeline outputs ───────────────────────────
craft_ocr_pipeline/outputs/
craft_ocr_pipeline/logs/

# ── Input data ────────────────────────────────
craft_ocr_pipeline/inputs/

# ── OS / editor ───────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# ── Windows installers ────────────────────────
*.exe
