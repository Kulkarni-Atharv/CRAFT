# ── CM5 requirements ──────────────────────────────────────────────────────────
# CRAFT runs via ONNX Runtime (no PyTorch needed).
# Recognition uses PaddleOCR — downloads its own models on first run (~190 MB).

# ── Camera ────────────────────────────────────────────────────────────────────
# Install via apt (NOT pip) — pip version lacks hardware drivers:
#   sudo apt install -y python3-picamera2

# ── CRAFT inference runtime ───────────────────────────────────────────────────
onnxruntime>=1.16.0          # ~50 MB  — runs craft.onnx (no PyTorch needed)

# ── Recognition ───────────────────────────────────────────────────────────────
paddlepaddle>=2.5.0          # ~400 MB  — PaddlePaddle CPU runtime for ARM64
paddleocr>=2.7.0             # ~50 MB   — downloads rec/cls models on first run

# ── Vision / numerics ─────────────────────────────────────────────────────────
numpy>=1.24.0                # ~30 MB
opencv-python>=4.8.0         # ~200 MB  (use opencv-python-headless to save ~20 MB)
scipy>=1.11.0                # ~50 MB  — connected-components in postprocessing
Pillow>=10.0.0               # ~5 MB

# ── Config / utils ────────────────────────────────────────────────────────────
PyYAML>=6.0                  # <1 MB
