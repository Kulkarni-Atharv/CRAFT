# ── CM5-specific ──────────────────────────────
picamera2>=0.3.12          # installed via apt on CM5, listed here for reference
# sudo apt install -y python3-picamera2

# ── Core (same as requirements.txt) ───────────
torch>=2.0.0               # use the ARM wheel for Pi:
                           #   pip install torch --index-url https://download.pytorch.org/whl/cpu
torchvision>=0.15.0
numpy>=1.24.0
opencv-python>=4.8.0       # or opencv-python-headless if no display
Pillow>=10.0.0
scipy>=1.11.0
PyYAML>=6.0

# ── OCR Recognition ───────────────────────────
paddlepaddle>=2.5.0        # CPU-only build — no GPU on CM5
paddleocr>=2.7.0
