# ── CM5 requirements ──────────────────────────────────────────────────────────
# Install with: pip install -r requirements_cm5.txt
# Camera support (outside venv): sudo apt install -y python3-picamera2

# ── CRAFT detection ───────────────────────────────────────────────────────────
onnxruntime>=1.16.0

# ── Recognition — PP-OCR models via ONNX Runtime (no PaddlePaddle needed) ────
rapidocr-onnxruntime

# ── Vision / numerics ─────────────────────────────────────────────────────────
numpy>=1.24.0
opencv-python-headless>=4.8.0
scipy>=1.11.0
Pillow>=10.0.0

# ── Config / utils ────────────────────────────────────────────────────────────
PyYAML>=6.0
