# ── CM5 requirements ──────────────────────────────────────────────────────────
# PaddlePaddle 3.0 has proper ARM64 support. Install in this exact order:
#
#   1. pip uninstall paddlepaddle paddleocr -y
#   2. pip install paddlepaddle==3.0.0 \
#        -i https://www.paddlepaddle.org.cn/packages/stable/cpu-aarch64/
#   3. pip install paddleocr==3.4.1
#   4. pip install -r requirements_cm5.txt
#
# ── Camera ────────────────────────────────────────────────────────────────────
# sudo apt install -y python3-picamera2

# ── CRAFT inference ───────────────────────────────────────────────────────────
onnxruntime>=1.16.0

# ── Recognition — install paddle BEFORE running this file (see above) ─────────
paddleocr==3.4.1

# ── Vision / numerics ─────────────────────────────────────────────────────────
numpy>=1.24.0
opencv-python-headless>=4.8.0
scipy>=1.11.0
Pillow>=10.0.0

# ── Config / utils ────────────────────────────────────────────────────────────
PyYAML>=6.0
