# ── CM5 requirements ──────────────────────────────────────────────────────────
# IMPORTANT: PaddleOCR requires Python 3.11 — create a pyenv venv first:
#
#   sudo apt install -y make build-essential libssl-dev zlib1g-dev \
#     libbz2-dev libreadline-dev libsqlite3-dev wget curl \
#     libncursesw5-dev xz-utils libffi-dev liblzma-dev libgl1
#   curl https://pyenv.run | bash
#   # add pyenv to ~/.bashrc then:
#   pyenv install 3.11.9
#   cd ~/CRAFT/CRAFT/craft_ocr_pipeline
#   ~/.pyenv/versions/3.11.9/bin/python -m venv venv
#   source venv/bin/activate
#   pip install -r requirements_cm5.txt
#
# ── Camera ────────────────────────────────────────────────────────────────────
# Install via apt (NOT pip) — pip version lacks hardware drivers:
#   sudo apt install -y python3-picamera2

# ── CRAFT inference runtime ───────────────────────────────────────────────────
onnxruntime>=1.16.0

# ── Recognition ───────────────────────────────────────────────────────────────
paddlepaddle>=2.6.0
paddleocr>=2.7.0

# ── Vision / numerics ─────────────────────────────────────────────────────────
numpy>=1.24.0
opencv-python-headless>=4.8.0
scipy>=1.11.0
Pillow>=10.0.0

# ── Config / utils ────────────────────────────────────────────────────────────
PyYAML>=6.0
