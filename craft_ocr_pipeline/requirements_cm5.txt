# ── CM5 lightweight requirements (~250 MB total) ──────────────────────────────
# PyTorch, torchvision, PaddleOCR and PaddlePaddle are NOT needed here.
# Both CRAFT and CRNN run via ONNX Runtime exported from the dev machine.

# ── Camera ────────────────────────────────────────────────────────────────────
# Install via apt (NOT pip) — pip version lacks hardware drivers:
#   sudo apt install -y python3-picamera2

# ── Inference runtime ─────────────────────────────────────────────────────────
onnxruntime>=1.16.0          # ~50 MB  — replaces torch + paddlepaddle entirely

# ── Vision / numerics ─────────────────────────────────────────────────────────
numpy>=1.24.0                # ~30 MB
opencv-python>=4.8.0         # ~200 MB  (use opencv-python-headless to save ~20 MB)
scipy>=1.11.0                # ~50 MB  — connected-components in postprocessing
Pillow>=10.0.0               # ~5 MB

# ── Config / utils ────────────────────────────────────────────────────────────
PyYAML>=6.0                  # <1 MB
